bin/
obj/

# Miljøspesifikk konfigurasjon: Tenor-brukere, syntetiske organisasjoner og andre verdier som
# hører til miljøet vi deployer mot, ikke til koden. Se appsettings.Local.example.json.
appsettings.Local.json

# Personlige Claude Code-innstillinger (tillatelsesregler o.l.) — ikke team-delt.
.claude/settings.local.json

# Lokale SQLite-databasefiler generert ved manuell/egen-port dev-server-kjøring under bakgrunnsagent-
# verifisering — aldri ment å versjoneres (samme rolle som en lokal .db-cache).
*.db
*.db-shm
*.db-wal
